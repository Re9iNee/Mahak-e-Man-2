console.group('carousel.js')
// $('.carousel').carousel('pause');
// $('.carousel').on('slide.bs.carousel', function () {
//     console.error("slide");
// })
// $('.carousel').on('slid.bs.carousel', function () {
//     console.error("sleeed");
// })
// $('.carousel').carousel('pause');
// const slides = ["بازی", "معرفی"];
// let currentSIndex, prevSIndex, nextSIndex;
// currentSIndex = 0;
// prevSIndex = currentSIndex == 0 ? slides.length - 1 : currentSIndex - 1;
// nextSIndex = currentSIndex < slides.length ? currentSIndex + 1 : 0;
// setInterval(() => {
//     $('#prev').html(slides[prevSIndex]);
//     $('#next').html(slides[nextSIndex]);
//     $('.carousel').carousel('next');
//     debugger
//     if (currentSIndex < slides.length - 1) {
//         increment()
//     } else {
//         reset()
//     }
// }, 2000);


// function reset() {
//     currentSIndex = 0;
//     prevSIndex = currentSIndex == 0 ? slides.length - 1 : currentSIndex - 1;
//     nextSIndex = currentSIndex < slides.length ? currentSIndex + 1 : 0;
// }

// function increment() {
//     prevSIndex = currentSIndex;
//     nextSIndex = ++currentSIndex;
// }
console.groupEnd()